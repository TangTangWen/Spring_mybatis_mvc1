<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.tang.mapper.BbsDetailMapper">

	<select id="selById" parameterType="int" resultType="cn.tang.pojo.BbsDetail">
		select * from bbs_detail where sortId=#{0}
	</select>
</mapper>